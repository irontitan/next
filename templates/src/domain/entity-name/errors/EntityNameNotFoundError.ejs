import { <%= entityNames.pascalCase %>Error } from './<%= entityNames.pascalCase %>Error'
import { format } from 'util'


export class <%= entityNames.pascalCase %>NotFoundError extends <%= entityNames.pascalCase %>Error {
  static MESSAGE = '<%= entityNames.singular(entityNames.sentenceCase) %> with ID "%s" was not found'

  constructor (id: string) {
    super(format(<%= entityNames.pascalCase %>NotFoundError.MESSAGE, id))
  }
}
