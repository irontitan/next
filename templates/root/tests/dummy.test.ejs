import { expect } from 'chai'
import { describe, it, before } from 'mocha'
import axiosist from 'axiosist'
import merge from 'lodash.merge'
import { config } from '../src/app-config'
import { app } from '../src/presentation/app'

// Uncomment this to run the test
// const options = merge({}, config, {
//   // Your custom config goes here
// })

// before(async () => {
//   const appMock = await app(options, 'test')
//   api = axiosist(appMock)
// })


describe('Bigger scope here', () => {
  it('Should deal with smaller scope here', async () => {
    // Example code
    // const response = await api.post('/', { wrong: 'object' })
    // expect(response.status).to.be.equal(422)
    expect(1).to.equal(1)
  })
})
